# Onboarding Decisions

- Anonymous use is allowed before account creation.
- The first profile writes only low-friction personalization fields.
- `anchor_user_id` is stored locally until auth/account linking exists.
- The user is routed into action immediately instead of a dashboard tutorial.

## Deferred

- Magic-link auth.
- Apple/Google account linking.
- Migration path from anonymous user to authenticated user.
